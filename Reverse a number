# Input a number
num = int(input("Enter a number: "))
reverse = 0
n = num

# Reverse the number
while n > 0:
    digit = n % 10
    reverse = reverse * 10 + digit
    n //= 10

print("Reversed number is:", reverse)
