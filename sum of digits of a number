# Input a number
num = int(input("Enter a number: "))
sum_digits = 0
n = num

# Calculate sum of digits
while n > 0:
    digit = n % 10
    sum_digits += digit
    n //= 10

print("Sum of digits of", num, "is:", sum_digits)
